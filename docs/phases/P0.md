# Phase P0 — Runtime & Boot

**Exit criteria:** `./jackal.sh` boots, authenticates, 3+ prompt cycles, disposes cleanly; `jackal run --check` passes.

## Tasks

| ID | Task | Status |
|----|------|--------|
| P0-1 | Headless adapter (`createNextAgent`) | done |
| P0-2 | Agent loop (`JackalAgentSession`) | done |
| P0-3 | Immutable store + bridge | done |
| P0-4 | Auth + models | done |
| P0-5 | System prompt from `jackal/SYSTEM.md` | done |
| P0-6 | Project CWD (`JACKAL_AGENT_CWD`) | done |
| P0-7 | Graceful dispose (SIGINT polish) | partial |
| P0-8 | Smoke/CI: `node dist/index.js --check` or `jackal run --check` | **todo** |
| P0-9 | Lazy MCP spawn after first TUI frame | **todo** |

## Implementation notes

- **P0-8:** Add `src/cli.ts` + export from `index.ts`; `jackal.sh run --check` calls `runNextAgentSmoke(cwd)` without Ink.
- **P0-9:** Defer `session.connectMcp()` until `requestAnimationFrame` equivalent or 100ms after `markReady()`; shell shows "MCP connecting…" in status bar.

## Nanocoder ref

- `reference/nanocoder/source/cli.tsx` — boot flags
- `reference/nanocoder/source/cli-harness.spec.ts` — smoke patterns

# Phase P2 — Safety + Context Input

**Exit criteria:** Normal/auto-accept modes work; `@src/foo.jac` injects context; destructive bash gated in auto-accept.

## Tasks

| ID | Task | Status |
|----|------|--------|
| P2-1 | `mode` in store: normal \| auto-accept \| yolo \| plan | **done** |
| P2-2 | Shift+Tab cycle modes; status bar indicator | **done** |
| P2-3 | `--mode` CLI flag | **done** |
| P2-4 | Tool approval queue + Ink confirm dialog | **done** (`ToolConfirmationOverlay` + `approval-display.ts`) |
| P2-5 | Destructive bash/git detection (auto-accept) | **done** |
| P2-6 | Plan mode tool filter (port `plan-mode.ts`) | **done** |
| P2-7 | `@file` fuzzy autocomplete | **done** (`completions.ts` + cursor-aware mention) |
| P2-8 | `@file:10-20` line ranges | **done** (`file-mention-parser.ts`, `context-input.ts`) |
| P2-9 | `!command` inline bash → context | **done** |
| P2-10 | `/explorer` multi-select overlay | **done** |
| P2-11 | Token estimate on large `@` selections | **done** |

## Implementation notes

- `@` / `!` expansion: `src/workflow/context-input.ts`
- Mention parsing: `src/workflow/file-mention-parser.ts`
- File listing + gitignore: `src/project/file-explorer.ts`, `src/project/gitignore.ts`
- Autocomplete: `src/ui/completions.ts` (passes cursor from shell via facade)

## Nanocoder ref

- `reference/nanocoder/docs/features/development-modes.md`
- `reference/nanocoder/source/utils/file-mention-parser.ts`

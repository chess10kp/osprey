# Phase P7 — Jac Toolchain MVP

**Exit criteria:** `/jac-check` shows diagnostics; `/fix` resolves broken `.jac` within retry cap.

## Tasks

| ID | Task | Status |
|----|------|--------|
| P7-1 | `/jac-doctor` full detection report | partial |
| P7-2 | `/jac-check` structured `JacDiagnostic[]` | partial |
| P7-3 | `/fix` bounded loop with diff preview | partial |
| P7-4 | Autocheck on edit | done |
| P7-5 | `/jac-test`, `/jac-format`, `/jac-run` | **todo** |
| P7-6 | `runJacCommand()` unified parser | **todo** |
| P7-7 | Explain slash variants + prompts | **todo** |
| P7-8 | `/create` template picker | partial |
| P7-9 | Project skill injection | **todo** |

## Types

```ts
type JacDiagnostic = {
  file: string;
  line: number;
  column?: number;
  severity: "error" | "warning" | "info";
  code?: string;
  message: string;
  raw: string;
};
```

## Legacy port

Completed in consolidation PR1 — use `src/runtime/jac-cli.ts`, `src/runtime/jac-doctor.ts`, and `templates/shell.cl.jac` slash handlers.

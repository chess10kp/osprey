# Phase P6 — Non-Interactive & Automation

**Exit criteria:** `jackal run "prompt"` exits 0 on success; `--plain` for CI; exit 1 when approval blocked.

## Tasks

| ID | Task | Status |
|----|------|--------|
| P6-1 | `jackal run "prompt"` headless | **done** |
| P6-2 | `--mode plan|yolo|auto-accept` | **done** |
| P6-3 | `--plain` pipe-friendly output | **done** |
| P6-4 | Exit code 1 on approval required | **done** |
| P6-5 | Optional `/schedule` (defer) | deferred |

## Nanocoder ref

- `reference/nanocoder/docs/features/commands.md` § Non-Interactive Mode

[project]
name = "jackal-shell"
version = "0.1.0"
description = "Jackal Ink shell — wired to the headless Pi adapter via @jac/pi"
entry-point = "templates/shell.cl.jac"

[dependencies.npm]
ink = "^7.0.3"
react = "^19.2.4"
"@inkjs/ui" = "*"

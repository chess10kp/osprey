"""Retired legacy smoke: OSP region painter (pre Unit 6 retained renderer).

Use virtual-terminal renderer tests instead:
  cd app && JAC_TEST_JOBS=0 jac test ui/renderer.test.jac ui/layout.test.jac
"""

with entry {
    print("osp-region-smoke: retired — see app/ui/renderer.test.jac");
}

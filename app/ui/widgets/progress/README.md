# ProgressBar external widget (Gate 5)

Implementation and companion tests live at `app/ui/progress_bar.jac` so
`jac test` companion discovery works (nested `widgets/progress/progress.test.jac`
was not collected). This directory is the package home for Gate 5 authoring;
import from `ui.progress_bar` until a nested package layout is supported.

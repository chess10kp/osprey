# Explain Jac Graph

Analyze the following Jac code and describe its **graph data model**:

1. **Nodes** — list each node type with its fields and purpose
2. **Edges** — list each edge type, direction, and what relationship it models
3. **Graph shape** — describe the overall graph topology (tree, DAG, mesh, etc.)
4. **Walkers** — which walkers traverse the graph and how
5. **Suggested visualization** — describe a Mermaid or ASCII diagram that would illustrate this graph

```jac
{{code}}
```

If the code does not define a graph model (no nodes/edges), explain whatever graph-relevant constructs exist and suggest how to model the domain as a graph.

# Explain Walker

Explain the following Jac walker in detail:

1. **What it does** — one-sentence purpose
2. **Graph traversal** — where it spawns, which nodes/edges it visits, and why
3. **State mutations** — what `has` fields change during traversal
4. **Abilities** — which entry/exit abilities fire and their effects
5. **Edge traversal semantics** — `ignore`, `take`, `disengage`, etc.
6. **Result** — what the walker returns or produces

```jac
{{code}}
```

If the code is not a walker, explain the closest relevant Jac construct instead.

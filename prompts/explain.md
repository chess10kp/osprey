# Explain Jac

Explain the following Jac code in terms of its graph-based architecture:

- Which parts are nodes, edges, walkers, abilities
- How traversal works
- What the object-spatial model looks like
- How AI-native constructs (like `by llm()`) are used

```jac
{{code}}
```
